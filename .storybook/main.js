module.exports = {
  stories: ["../src/**/*.stories.?(tsx)"],
  addons: [
    // TODO: possibly install + add these back in the future, if needed
    // "@storybook/addon-ondevice-notes",
    // "@storybook/addon-ondevice-controls"
  ],
};
